services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: nutrition
      POSTGRES_USER: user
      POSTGRES_PASSWORD: 22305
    ports:
      - "5488:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data: